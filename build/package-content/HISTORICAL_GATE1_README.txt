C3 1.3.0 historical Gate 1 runtime kit

This is compatibility-laboratory input, not a C3 release distribution.

The three executables are the exact official public runtime oracles. Verify
SHA256SUMS.txt before executing them. Use a disposable Windows 7 SP1 x64 image
with both .NET Framework 4.0 Full and 4.6 available so v1.1.2, v1.2.0 Beta 1
x86, and v1.2.0 Beta 1 x64 can execute in one isolated environment.

Keep networking blocked except for a separately controlled update-check test.
Use an isolated user profile. Never overwrite the retained input catalogue.
Record the exact OS/framework state, executable hashes, workflow results,
screenshots, private-catalogue hashes, and all nine writer-to-reader exchange
cells in docs/testing/1.3.0-historical-gate1-record.md.

The 27 workflow rows are observation/classification evidence. Record a known
historical defect as reproduced or explicitly not reproduced and bind it to its
APP-nnn ledger row; do not manufacture a false workflow PASS. The nine exchange
cells must still preserve all represented catalogue data.

Passing this kit does not authorize an Alpha/Beta tag, distribution, public
release, feed change, or legacy branch movement.
