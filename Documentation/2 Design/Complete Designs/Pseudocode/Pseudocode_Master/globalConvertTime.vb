'Convert time between readable string format and computable integer format
Public Function convertTime

	if mode = "integer" then
		
		return time as number of minutes since midnight

	else

		return time as digital time 'eg 3:00 PM

End Function