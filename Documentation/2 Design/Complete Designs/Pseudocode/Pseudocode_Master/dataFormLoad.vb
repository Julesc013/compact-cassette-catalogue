'Load in employee data upon form load (and count how many employees)
Private Sub when data form loads

	dataCount = 0 'reset each time form is loaded

	'populate list box
	for each record in employee data structure

		populate all list boxes
		increment dataCount by 1
		
	end for

End Sub