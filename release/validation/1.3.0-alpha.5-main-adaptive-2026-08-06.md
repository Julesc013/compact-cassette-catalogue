# C3 1.3.0 Alpha 5 Main adaptation — 6 August 2026

Result: **PASS for the Main Stage B adaptive source change.**

The Stage A screenshot at 800×552 showed that the reparented but still
absolute Find controls could extend outside `grpFind`. Stage B replaces those
last fixed sibling relationships with:

```text
grpFind
└── tlpFindFields
    ├── txtTerm       (elastic)
    ├── cmbField      (bounded selector)
    └── btnFind       (autosized command)

grpIdentification
└── tlpIdentificationFields
    ├── Short label/value
    ├── Long label/value
    └── flpIdentificationNumber
        └── Number label/current/slash/total
```

The 800-pixel screenshot now retains all Find and Identification controls and
keeps Identification, Scroll, and Actions as nonintersecting siblings. The
maximum-text 200% observation preserves the same order and reachability at the
WinForms-scaled minimum. A new characterization case sets `frmMain` to its
800×600 minimum, proves every field belongs to its semantic layout container,
and checks that every child rectangle is contained by that parent.

The closed control-name allow-list contains exactly 25 reviewed additions and
zero baseline removals. All 135 characterization cases pass. The complete
commit-bound Main JSON/screenshot atlas and native DPI, keyboard, High
Contrast, and assistive-technology review remain evidence gates, so the ledger
state is `adaptive-pass`, not `qualified`.

No release identity, package, tag, publication, feed, or protected branch
changed.
