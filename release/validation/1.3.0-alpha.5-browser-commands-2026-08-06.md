# C3 1.3.0 Alpha 5 browser commands — 6 August 2026

Result: **PASS for the four-browser Designer-ownership slice.**

The following formerly runtime-created commands now exist once in the owning
Designer and use compile-time `Handles` event wiring:

```text
frmTapes   btnAddTape
frmModels  btnAddModel
frmBrands  btnAddBrand
frmDecks   btnAddDeck
```

Each command retains its catalogue workflow call, mnemonic, accessible name,
accessible description, and list refresh after successful creation. The old
button fields, literal rectangles, `AddHandler` calls, `grpActions.Top/Height`
mutations, and per-child `Top` loops are gone from behavior source.

For this structural commit, the prior post-Load action-stack positions were
projected exactly into Designer source: the new Add action occupies the first
row and the existing Refresh/Edit/Delete/status controls retain their relative
rows. Responsive browser reconstruction remains a separate Stage B change.

The exact control-name allow-list now closes 11 additions with zero baseline
removals. A new characterization case instantiates all four browsers, proves
one Add control per form, checks mnemonic/accessibility metadata, and rejects
a return of runtime Add rectangles or action-group movement.

All 132 characterization cases pass. The strengthened source-policy inventory
now reports 17 open findings in seven files, down from the retained Alpha 4
baseline of 52 in ten. No release identity, package, tag, feed, or protected
branch changed.
