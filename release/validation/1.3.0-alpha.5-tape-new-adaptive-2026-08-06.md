# C3 1.3.0 Alpha 5 Tape New adaptation — 6 August 2026

Result: **PASS for the Tape New Stage B adaptive source change.**

The fixed controls inside `grpModel` now belong to one local table:

```text
tlpTapeModelFields
├── Model label | elastic model choice | Add Model…
├── Year label  | year | Length label | length
└── Region label| elastic region choice
```

The existing controls, values, tab intent, and detour handler are retained.
The model and region ComboBoxes fill their assigned cells, Add Model autosizes
to its command cell, and the two numeric fields remain bounded.

A new characterization case sets `frmTapeNew` to 800×600, proves all nine
Model controls are parented by and contained within the local table, and
rechecks Add Deck, status text, Add Tape, and Cancel ownership in the
persistent command region. The closed control-name allow-list now contains 32
reviewed additions with zero baseline removals. Focused fresh-process ordinary
and 200% maximum-text observations at 800×552 report no geometry assertion
failure.

All 137 characterization cases must pass before accepting the source commit.
The commit-bound 32-cell Tape atlas and native DPI, keyboard, High Contrast,
and assistive-technology review remain evidence gates; the ledger state is
`adaptive-pass`, not `qualified`.

No release identity, package, tag, publication, feed, or protected branch
changed.
