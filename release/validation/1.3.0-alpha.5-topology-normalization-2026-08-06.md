# C3 1.3.0 Alpha 5 topology normalization — 6 August 2026

Result: **PASS for the bounded source-topology transaction.**

This record closes Phase 0 of the Alpha 5 plan. It does not claim layout
repair, regenerated application/setup binaries, an Alpha 5 identity, package,
tag, publication, feed promotion, or movement of `master`, `legacy/1.x`, or
`dev/2.x`.

## Boundary and change

The transaction began from clean synchronized `dev/1.x` commit `ac6ac01`.
It moved the existing 13-file shared setup engine, without content edits, from
root `SetupShared/` to:

```text
Compact Cassette Catalogue Installer/Shared/
```

The Installer compiles those files locally. The Uninstaller and
`C3.Setup.Characterization` compile the same resolved physical files through
old-style linked `Compile` items. No shared DLL, duplicate implementation, new
project, runtime dependency, or root `src/` production tree was introduced.

## Mechanical authority

`build/setup-shared-source-contract.json` retains the exact 13 accepted names
and their pre-move SHA-256 values. `build/validate-1x-source-topology.ps1`
requires:

- exactly the three legacy production-source roots;
- no physical root `src/` or `SetupShared/` directory;
- the exact file-name and hash closure;
- one resolved physical source for all three consumers;
- canonical Installer, Uninstaller, and test include/link projections; and
- no tracked production VB source outside the three product roots or tests.

The complete preparation verifier now runs that contract immediately after
the baseline-genome gate.

## Verification

The post-move tree passed:

```text
build/validate-1x-source-topology.ps1
build/validate-setup-offline.ps1
build/validate-setup-journal.ps1
build/validate-setup-genome.ps1
build/verify-preparation.ps1 -SkipBuildOutputs
git diff --check
```

The complete verifier re-executed all 130 characterization cases (31
application, 8 integrity, 6 settings, and 85 setup) and revalidated the source
genome, catalogue corpus, documentation, lane, package, and setup-control
authorities. Any compiler output produced later is new Alpha 5 evidence and
must not be confused with immutable Alpha 4 bytes.

## Release boundary

Repository release identity remains `1.3.0a4 / Alpha 4`. `v1.3.0a4` is not
moved or rewritten. The topology pass authorizes the geometry-characterization
phase only; it does not by itself earn `1.3.0a5`.
