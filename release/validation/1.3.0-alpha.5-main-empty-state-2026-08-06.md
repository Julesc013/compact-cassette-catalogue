# C3 1.3.0 Alpha 5 Main empty state — 6 August 2026

Result: **PASS for Designer ownership of the Main empty-catalogue surface.**

`frmMain.Designer.vb` now owns exactly one instance of each static element:

```text
pnlEmptyCatalogue
lblEmptyCatalogue
btnAddFirstTape
```

The panel continues to occupy the existing editor bounds for this bounded
structural commit. Its visibility is driven only by catalogue state, and the
button retains the guided Tape workflow through a compile-time `Handles`
clause. Behaviour source no longer creates the panel, label, button, or fixed
rectangle; adds them to the form; or changes their z-order during `Load`.

This is deliberately not the Main layout qualification. The top-level
menu/header/editor hierarchy, removal of form-level scrolling, adaptive editor
viewport, and measured collision fixes remain in the separate structural and
adaptive Main commits.

The closed Alpha 5 control-name allow-list now contains exactly 14 reviewed
additions and reports zero baseline removals. A new characterization case
instantiates `frmMain`, proves the three controls are unique and correctly
parented, checks the Add First Tape mnemonic and accessibility description,
and rejects the former runtime construction and `BringToFront` mechanism.

All 133 characterization cases pass. The remaining source-policy inventory is
13 findings in seven files, down from the immutable Alpha 4 discovery baseline
of 52 findings in ten files. No release identity, package, tag, publication,
feed, or protected branch changed.
