# C3 1.3.0 Alpha 2 repository preparation

Date: 5 August 2026

Status: Alpha 2 Candidate build, package, reproducibility, adversarial, and
builder-smoke gates passed; evidence commit `E` is ready for the authorized
annotated tag and post-tag attestation.

## Scope completed in this record

The active release manifest now projects one closed identity across all three
lanes:

```text
releaseVersion=1.3.0
releaseStage=Alpha 2
releaseLabel=1.3.0a2
releaseTag=v1.3.0a2
releaseChannel=alpha
publicationStatus=retained-unpublished
assemblyVersion=1.3.0.0
fileVersion=1.3.0.0
assemblyProductVersion=1.3.0a2
```

The package authority is stage-specific:

```text
C3-v1.3.0a2-win-x86-net40-portable.zip
C3-v1.3.0a2-win-x64-net48-portable.zip
C3-v1.3.0a2-win-arm64-net481-portable.zip
SHA256SUMS.txt
```

Packaging and verification now bind that identity through `BUILD.txt`, retained
entry manifests, the PowerShell 2 target projection, target runtime evidence,
and cross-lane release/source/lock closure. A mixed-label set or a stable-looking
Alpha package set is rejected mechanically. Numeric CLR/file metadata remains
`1.3.0.0`; assembly informational/product metadata is `1.3.0a2`.

## Evidence boundary

The earlier sections below retain the chronology of source-only preparation.
The final Candidate run now claims the three retained Alpha 2 portable ZIPs,
one external immutable lock, exact clean-source reproduction, and x86/x64
builder smoke from package source `C`:

The exact historical markers `repository identity preparation in progress`,
`does not claim that the three packages`, and
`Administrator action is required` described that earlier state. They are
retained here as historical state, not as the present result.

```text
0c740ec11865a4df6364b2f430ce2feece58d9d6
```

This evidence does not claim native ARM64 execution, minimum-target-OS
qualification, historical Gate 1 completion, application runtime repair, or
public publication. At evidence commit `E`, `v1.3.0a2` is still absent by
construction; the annotated tag is created only after `E` is committed.

## Clean-HEAD source-only attestation

The complete Alpha 2 source-only validator passed from clean commit:

```text
1685c63a657cf7941caeda2f959de181091cee56
```

Executed command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File build\verify-alpha2.ps1 -SkipBuildOutputs
```

Recorded results:

- baseline genome: PASS, 13 frozen surfaces and zero approved differences;
- compatibility corpus: PASS, 10 public releases and five supported producers;
- standalone XML characterization: PASS, eight cases;
- documentation and local links: PASS, 35 tracked Markdown files;
- three-lane and Alpha 2 release identity projection: PASS;
- public 1.2 feed isolation and immutable Alpha 1/legacy identities: PASS;
- cross-lane release/source/lock negative tests: PASS, including mixed and
  stable-looking Alpha identity rejection; and
- actual target tooling under Windows PowerShell 2: PASS.

This is source-only preparation evidence. It does not convert any stale build
output into Alpha 2 evidence and does not satisfy the build/package/tag gate.

Post-review clean-HEAD rerun `e6294e01658bcf67aec5c01d480ab438bb254f6a`
also passed the same complete source-only command. That descendant fixes the
tagged-mode evidence boundary: Candidate mode performs reproduction and
adversarial work at package source `C`, while tagged mode verifies the retained
bytes against `C` without rebuilding them from evidence-only commit `E`.

## Post-tag topology and reproducibility correction

Clean source commit `7b2199bb3bd1da68d5ffd4d10c3e7a550c473eaf`
passed the complete source-only Alpha 2 validator after the final record controls
were added:

- qualification hashes are frozen in a machine-readable record at `E`;
- annotated `v1.3.0a2` remains at `E`;
- direct child `P` changes only the dedicated post-tag JSON record;
- `PostTag` mode requires `P^ = E`, verifies the annotated local and remote tag
  object/target, and compares retained bytes with the hashes frozen at `E`;
- the tag message must contain every required unpublished/deferred fragment;
- Alpha/Beta/stable channel, label, stage, and ordinal relationships fail
  closed, including an internally consistent stable-looking Alpha negative;
- Alpha reproducibility invokes two clean, path-distinct Candidate source
  rebuilds and retains both build/log evidence sets; repeated packaging of one
  output set is not accepted as source reproducibility; and
- package, entry-manifest, checksum, Candidate closure, source-rebuild,
  toolchain-evidence, and binary-log hashes are bound before tag creation.

The new qualified and post-tag JSON files remain templates. No package hashes,
tag object, remote tag, external lock, Candidate result, or Alpha distribution
is claimed by this source-side pass.

## Serviced builders and Candidate result

The administrator servicing dependency was completed before lock capture. Each
installation reports complete, stable, launchable, and no reboot required:

| Builder | Installed product version | Installation version | Required floor |
| --- | --- | --- | --- |
| Visual Studio 2017 | `15.9.81` | `15.9.37506.8` | `15.9.81` |
| Visual Studio 2022 | `17.14.37 (July 2026)` | `17.14.37516.0` | `17.14.37` |
| Visual Studio 2026 | `18.8.2` | `18.8.12023.21` | `18.8.2` |

The first serviced build found and corrected Windows PowerShell native-argument
quoting for space-containing directory properties. The first path-distinct
source comparison then found path/EOL-dependent package bytes. Commit
`0c740ec11865a4df6364b2f430ce2feece58d9d6` closed that failure by enforcing
deterministic, path-mapped, symbol-free Release compilation and canonical LF
package text. The complete Candidate gate then passed with lock SHA-256:

```text
f741b6777206d3531dd89b3f2e579ec9ef8ba3b45af7285a342fa98cd0d0107a
```

Retained portable assets:

| Asset | SHA-256 |
| --- | --- |
| `C3-v1.3.0a2-win-x86-net40-portable.zip` | `1ed41a2026d3b566458e1a010dd6c7f4e459d310d6316e50e50ff822424eca35` |
| `C3-v1.3.0a2-win-x64-net48-portable.zip` | `a65e59cef02a20559c0e787d8cdf332a52500f6e577bf6a585896370cf139fda` |
| `C3-v1.3.0a2-win-arm64-net481-portable.zip` | `9cbaaa237204b285d94887162ecb5f5e86560ba53776e4d3f90611e98661a39c` |

Both clean, path-distinct source builds reproduced those exact package and
entry-manifest bytes. The adversarial release-control suite passed. Builder
smoke launched and normally closed the x86 and x64 packages; ARM64 execution
was correctly skipped on the x64 host and remains a target-machine gate.

## Deferred without waiver

- historical VS2015 Gate 1 reconstruction;
- all application runtime repairs;
- exact target-OS workflows;
- native ARM64 execution proof;
- public Alpha publication;
- every Beta-labelled operation;
- stable feed promotion; and
- `legacy/1.x` movement.

The inherited recursive-close reproduction remains assigned to the later
lifecycle pair and is not misclassified as a build-control failure.
