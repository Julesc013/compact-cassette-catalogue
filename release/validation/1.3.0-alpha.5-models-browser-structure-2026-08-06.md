# C3 1.3.0 Alpha 5 Models browser structure — 6 August 2026

Result: **PASS for the Models browser Stage A hierarchy.**

`frmModels` now follows the same fixed-filter/flexible-results contract as the
Brands browser. Its SplitContainer owns a scrollable filter pane and a result
pane whose table gives the Models ListView all remaining space above a
persistent status/actions footer. The ListView is docked Fill; the form itself
does not scroll.

The original filters, result columns, behavior, and action group are retained.
Add/Refresh/Edit/Delete tab indexes are unique, and behavior source no longer
invokes `CatalogueUx.ConfigureListForm`.

A new characterization case closes the Model-specific hierarchy and helper
removal. The exact control-name allow-list contains 40 reviewed additions with
zero baseline removals. The focused 800×552 process passes current geometry
assertions; all 139 characterization cases must pass before accepting the
source commit.

Adaptive action-row work, the family atlas, and native review remain open. No
release identity, package, tag, publication, feed, or protected branch changed.
