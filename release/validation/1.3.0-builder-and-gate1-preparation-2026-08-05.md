# C3 1.3.0 builder and Gate 1 preparation — 5 August 2026

Result: PARTIAL preparation PASS; builder servicing and historical installation
BLOCKED by the current non-administrator execution token

Validated source:
`0acc8b66d3b96f94524d67b8288e1003b6687385`

At the retained run, local `HEAD` and `refs/remotes/origin/dev/1.x` were exactly
that commit and the worktree was clean. No build output was regenerated because
the authoritative builder-update prerequisite did not complete. No lock,
Candidate, Alpha distribution, Beta distribution, tag, publication, feed, or
`legacy/1.x` claim is made.

## Current servicing authority

Official Microsoft history available on the decision date records:

| Family | Latest stable/current release | Release date |
| --- | --- | --- |
| Visual Studio 2017 15.9 | 15.9.81 | 14 July 2026 |
| Visual Studio 2022 17.14 | 17.14.37 | 22 July 2026 |
| Visual Studio 2026 Stable | 18.8.2 | 28 July 2026 |

The earlier supplied VS2022 pin of 17.14.36 was stale: 17.14.37 already existed
before 5 August. Commit `6980447` corrected `build/lanes.json`, mechanically
closed all three starting pins in `validate-lanes.ps1`, and updated current
governance/evidence before any acceptable lock or Candidate build existed.

Sources:

- [Visual Studio 2017 15.9 release notes](https://learn.microsoft.com/en-us/visualstudio/releases/2017/vs2017-relnotes)
- [Visual Studio 2022 release history](https://learn.microsoft.com/en-us/visualstudio/releases/2022/release-history)
- [Visual Studio 2026 release history](https://learn.microsoft.com/en-us/visualstudio/releases/2026/release-history)

## Installed builder inventory

`vswhere` still reports complete, launchable, stable-channel instances with no
instance-level reboot requirement:

| Lane authority | Installed product | Installation version | Required minimum |
| --- | --- | --- | --- |
| x86/net40 | VS2017 15.9.80 | 15.9.37327.18 | 15.9.81 |
| x64/net48 | VS2022 17.14.35 | 17.14.37411.7 | 17.14.37 |
| ARM64/net481 | VS2026 18.7.2 | 18.7.11919.86 | 18.8.2 |

No IDE, MSBuild, compiler-server, or installer process was running at preflight.
System drives had ample free space. A pre-existing pending-file-rename entry was
limited to Windows Amcache and Edge Update paths; `--norestart` was retained.

Microsoft documents that quiet/passive Visual Studio updates require an
administrator-elevated process. The quiet VS2017 update attempt returned 5007
without changing the instance; its log stated that quiet/passive commands must
start elevated. The Codex token is a medium-integrity member of
`CodexSandboxUsers`, not `BUILTIN\Administrators`, and cannot supply administrator
credentials.

A standard-user UI attempt subsequently created elevated `setup.exe` process
31900 but made no progress: it retained no window handle, wrote a zero-byte log,
used no continuing CPU, and left VS2017 at 15.9.80. That exact process rejected
termination from the non-administrator token. An administrator must close it
before retrying the updates.

After that cleanup, run these commands from an Administrator PowerShell or
Command Prompt, one at a time, then re-inventory with `vswhere`:

```text
"C:\Program Files (x86)\Microsoft Visual Studio\Installer\setup.exe" update --installPath "C:\Program Files (x86)\Microsoft Visual Studio\2017\Enterprise" --quiet --norestart
"C:\Program Files (x86)\Microsoft Visual Studio\Installer\setup.exe" update --installPath "C:\Program Files\Microsoft Visual Studio\2022\Enterprise" --quiet --norestart
"C:\Program Files (x86)\Microsoft Visual Studio\Installer\setup.exe" update --installPath "C:\Program Files\Microsoft Visual Studio\18\Enterprise" --quiet --norestart
```

This follows Microsoft's
[documented command-line update form](https://learn.microsoft.com/en-us/visualstudio/install/command-line-parameter-examples).

## Historical Gate 1 input

No VS2015/MSBuild 14 installation or local installer/layout existed. The
official Microsoft Build Tools 2015 Update 3 endpoint remained available, so
the bootstrapper was downloaded into ignored compatibility-lab artifacts and
verified:

```text
File:            buildtools_full.exe
Length:          29,387,928 bytes
File/product:    14.0.25420.1
SHA-256:         243eace2d62a208048f562c7f6ffed1912dbfd254d7f9ee00c7d760b36b42416
SHA-1:           0e375822c41300c9cbd14ddf21e97e91f6fed3f5
Signature:       Valid
Signer:          Microsoft Corporation
Thumbprint:      3BDA323E552DB1FDE5F4FBEE75D6D5B2B187EEDC
```

Commit `0acc8b6` added the pinned
`build/historical-toolchain.json` contract and
`build/prepare-historical-toolchain.ps1`. Positive verification passed. A
one-byte mutation was rejected, an outside-artifacts destination was rejected,
and temporary test data was removed. The installer is lab input only: it is not
installed, has not yet proved that its retired web payload remains obtainable,
and has no C3 1.3 release authority.

Both official `v1.2.0b1` runtime oracles were reverified without download:

```text
205ba251175d5a6fa20a3ace6127a00e5d10d73ad30581032c8f09b20ceb7222  C3-v1.2.0b1-win-x86.exe
257ec9d0ea86f268d8328d71041e63eb379fc1809c91593db29d883359db747c  C3-v1.2.0b1-win-x64.exe
```

## Required continuation

1. An administrator closes process 31900 and services all three maintained
   Visual Studio instances.
2. Re-inventory exact installed versions and reboot only if an installer reports
   that it is required.
3. Discard stale Preparation outputs and rebuild all three lanes from clean,
   pushed source; rerun binary, genome, corpus, XML, package, PowerShell 2, and
   adversarial gates.
4. Capture a new external pre-Beta/control-path lock and exercise the complete
   one-lock Candidate control path. It cannot become the final Beta lock.
5. Install the pinned VS2015 historical toolchain in the compatibility lab,
   prove `MSBuild.exe` version 14.0.25420.1 and actual compiler inputs, and then
   complete the remaining Gate 1 workflows.

Runtime repairs remain prohibited until Gate 1 passes. Explicit human approval
is required before producing or tagging any Beta.
