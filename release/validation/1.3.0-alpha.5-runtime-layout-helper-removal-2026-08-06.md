# C3 1.3.0 Alpha 5 runtime layout helper removal — 6 August 2026

Result: **PASS for removal of the obsolete runtime geometry layer.**

All former `CatalogueUx` callers had already moved their visible controls,
parent hierarchy, sizing rules, and command wiring into Designer declarations.
The unreferenced module is now deleted from `CatalogueWorkflow.vb`, including:

```text
ConfigureMainForm
ConfigureTapeForm
ConfigureListForm
AddCancelButton
AddActionButton
AnchorIfPresent
FindRequiredControl
```

A characterization case rejects restoration of the module or any named helper.
The application still compiles as the original single EXE with the same
workflow coordinator, stable identities, validation results, and creation
contracts.

The source-policy inventory has fallen from the retained Alpha 4 discovery
baseline of 52 findings in 10 files to one finding in one file:
`frmDeckNew.vb` sets form-level `AutoScroll` during load. That item remains
owned by the dense Deck New/Edit layout tranche and is not hidden or waived.

This is source-only Alpha 5 preparation. No release identity, package, tag,
publication, feed, or protected branch changed.
