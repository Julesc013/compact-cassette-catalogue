# C3 1.3.0 Alpha 5 setup layout discovery — 6 August 2026

Status: **PASS as conditional-entry evidence; setup layout qualification remains open.**

This record binds the decision to reconstruct the classic setup presentation to
an observed failure rather than a speculative redesign. No application,
installer, or uninstaller production source changed in this tranche.

## Bound source and inputs

| Authority | Value |
| --- | --- |
| Source commit | `41bf3adeb8c5aa2faa7b25998f35196b95da7ee8` |
| STA harness SHA-256 | `acf01409bd50400af2fcc88dd9fc3e62a33d9b8e3b141fe3c4482e7737c8b751` |
| x86/net40 Installer SHA-256 | `2dacf80ee094e6677b8ddec33266fbdf49f23c9e8cc709615cdcf49904a8fb99` |
| x86/net40 Uninstaller SHA-256 | `ef7461727bdb4d0ecaf00bd3465fad4f2aafb910a53aa780d26863dfa80ee015` |
| Setup source-policy SHA-256 | `1642810bbd484e4b46f7795f08d6775c0be837102176aeacd94c6272d11572d7` |
| Matrix summary SHA-256 | `6c9585a899a2123d035483cfa0186d6a06769d7091cf380247320ba3ffae2706` |

The harness loads each setup executable in its own STA process, removes the
operational `Load` event delegates only inside that test process, selects one
explicit Designer-declared page, and performs no installation, removal,
registry, shortcut, elevation, or payload operation.

## Matrix

The matrix covers four client sizes (`800x552`, `1024x720`, `1366x728`, and
`1920x1040`), four relative font scales (`1.00`, `1.25`, `1.50`, and `2.00`),
and ordinary plus maximum representative content for these ten states:

- Installer introduction, options, ready, install, success, and failure;
- Uninstaller ready, uninstall, success, and failure.

| Result | Cells |
| --- | ---: |
| Passed | 120 |
| Failed | 200 |
| Total | 320 |

Every 100% cell passes. Every page family nevertheless enters repair because
its fixed content panel and separately anchored button panel intersect at 125%
or because content escapes a non-scrollable form/parent at scaled profiles.
Failures comprise 120 Installer cells and 80 Uninstaller cells. The source
policy independently reports zero runtime geometry mutation or visible-control
construction in setup form behaviour files, locating the defect in Designer
parentage and sizing rather than setup transaction code.

## Retained evidence

[`evidence/1.3.0-alpha5/setup-discovery`](evidence/1.3.0-alpha5/setup-discovery)
retains:

- the complete 320-cell geometry JSON set and hash-bearing matrix summary;
- the zero-finding setup source-policy result;
- one 800x552, 200%, maximum-content diagnostic screenshot for every state;
- `SHA256SUMS.txt` covering every retained file.

The remaining 310 screenshots are machine evidence under the ignored artifact
root; the reviewed diagnostic subset is sufficient to make the source-bound
failure mechanism durable without adding approximately 49 MiB of redundant
PNG data to Git history.

## Boundary

This is authorization to repair only the reproduced setup presentation
geometry. It is not DPI/High Contrast/assistive-technology owner proof, setup
transaction qualification, an Alpha 5 build or tag, publication authority, or
permission to modify `master`, `legacy/1.x`, `dev/2.x`, feeds, or releases.
