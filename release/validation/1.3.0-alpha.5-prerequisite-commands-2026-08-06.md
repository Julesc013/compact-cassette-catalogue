# C3 1.3.0 Alpha 5 prerequisite commands — 6 August 2026

Result: **PASS for the Model New and Tape New Designer-ownership slice.**

The following formerly runtime-created buttons are now explicit Designer
controls:

```text
frmModelNew: btnAddBrand, btnCancel
frmTapeNew:  btnAddModel, btnAddDeck, btnCancel
```

Their names, captions, mnemonics, accessible metadata, dialog results, tab
indices, and event bindings are inspectable before `Load`. Click events use
compile-time `Handles` clauses, and each static command exists once.

`frmModelNew`'s former `ClientSize`, group-width, and group-left mutations were
projected into its Designer representation. `frmTapeNew`'s Model width and
literal command rectangles were likewise projected into its Designer. Neither
behavior file calls a `CatalogueUx` geometry/control-construction helper.

The exact Alpha 5 control-name closure now contains seven reviewed additions
and zero baseline removals. The application characterization suite passes its
keyboard/detour/accessibility/draft tests against the Designer controls.

The strengthened source-policy scan also treats a behavior-layer
`AutoScroll =` assignment as a violation. After this slice it reports 37 open
findings across `CatalogueWorkflow.vb`, the four browser forms, `frmMain.vb`,
and the remaining `frmDeckNew.vb` form-scroll assignment. Those findings are
the next work; this record does not claim adaptive layout or Alpha 5 release
qualification.
