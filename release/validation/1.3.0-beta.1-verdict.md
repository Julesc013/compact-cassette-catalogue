# C3 1.3.0 Beta 1 verdict

Status: **NO-GO**

Package source C-beta: `bbebac288f4996939124f882d0e9febcf2e5bdae`

Portable Beta GO: **false**

Classic setup Beta GO: **false**

Overall Beta GO: **false**

Tag authorized: **false**

Legacy promotion authorized: **false**

Public GitHub release: not authorized

VERSION feed: unchanged

`master` and `dev/2.x`: unchanged

## Retained Candidate

The exact six-asset Candidate is retained at:

```text
artifacts/candidates/1.3.0b1/bbebac288f4996939124f882d0e9febcf2e5bdae
```

It is bound to external toolchain lock SHA-256
`e2a437a53cb8ec3eb2d025ef2e391d49c76366831de6526b40bf16678e626a15`
and Candidate index SHA-256
`ccf901da4ca7584e085728d68f3bb87209fc7bb5746167dbaaa7025ffef53b50`.

The retained assets are the three Beta-labelled portable ZIPs, the three
Beta-labelled classic setup ZIPs, their authenticated entry manifests, and the
closed `SHA256SUMS.txt`. They were rebuilt from one clean source commit with one
immutable external lock. Two clean, path-distinct source checkouts reproduced
the authoritative application and setup binaries, all six ZIPs, entry
manifests, and assembled distribution.

An earlier reproducibility attempt correctly failed because a long-lived
integration worktree contained a mixed line-ending checkout that changed setup
compiler output. Those setup ZIPs were rejected. Fresh detached checkouts
reproduced the canonical setup bytes, and only those canonical bytes are in the
retained Candidate.

## Passing controls

- Source, genome, compatibility-corpus, PowerShell 2, lane, and setup controls.
- 120 focused regressions: 21 lifecycle/persistence, 8 integrity, 6
  settings/diagnostics, and 85 setup durability/adversarial tests.
- Regression-backed APP001 through APP015 repairs.
- Three-lane Candidate compilation and binary/payload inspection, including
  genuine `0xaa64` ARM64 and zero application runtime DLLs.
- Portable and classic setup package closure.
- Authenticated ten-phase classic setup journal and recovery model.
- Exact clean-source reproducibility after the rejected mixed-checkout attempt.
- x86 builder smoke. An isolated x64 retry passed after the first bounded
  main-window wait timed out; this is builder smoke, not target qualification.

## Missing release evidence

- Historical Gate 1: 27 historical workflows, nine catalogue-exchange cells,
  representative private catalogues, and screenshot/control evidence.
- Application workflow qualification on exact Windows XP SP3 x86 with .NET 4
  Full, Windows 7 SP1 x64 with .NET 4.8, and Windows 11 build 22000 native ARM64
  with .NET 4.8.1.
- Classic setup install, verify, repair, rollback, and uninstall qualification
  on those same three exact targets.

The bounded environment inventory found no suitable exact target systems or
private historical catalogue evidence. Native ARM64 binary inspection is not a
substitute for execution on an exact ARM64 target.

## Consequence

This record is populated from the machine verdict. A complete GO would permit
the annotated Beta tag and lease-protected legacy-ledger promotion. This NO-GO
retains truthful Candidate evidence but permits neither operation. No
`v1.3.0b1` tag was created and `legacy/1.x` was not advanced.

Public release, feed changes, `master`, and `dev/2.x` remain outside this
authorization in both outcomes.
