# C3 1.3.0 Alpha 5 browser command rows — 6 August 2026

Result: **PASS for the automated browser-family Stage B tranche.**

Brands, Models, Decks, and Tapes retain their original action GroupBox and
behavioral command names, captions, events, enabled-state rules, and keyboard
order. Each GroupBox now owns one Designer-declared `flpBrowseCommands` with
Add, Refresh, Edit, and Delete in that order. The footer stacks result status
over the actions row, and each button has an explicit logical minimum instead
of inheriting its former full-group width.

The maximum-content injector no longer selects an invented entry in
stable-key-bound DropDownList controls. It retains their real loaded choice and
still exercises long editable text. The geometry runner now rejects any
visible command whose bounds escape a named command bar; this new assertion
caught and closed an initial 200% false pass caused by the old button widths.

Focused fresh-process cells first passed for all four forms at 800×552 with
maximum content at relative scales 1.00 and 2.00. The complete retained matrix
then passed all 128 cells: four forms, four sizes, four relative scales, and
ordinary/maximum content. All four commands remained inside their flow
container in every cell. The governed control-name exception grows by exactly
four FlowLayoutPanels, with no baseline control removal.

## Retained evidence

```text
source commit:
  cf2bae1534964055c014182e9084bba2e0cbf770

layout executable SHA-256:
  3216df4cbe33f6bdcc512a8020c621e287c696c84af9e4ff1db00ffef4e15115

summary SHA-256:
  c2d72600c6ffa48445146add86c514470747076f2f311fec744c7f818221a08e

cells: 128 passed / 0 failed
files: 128 JSON + 128 PNG + summary.json
root: release/validation/evidence/1.3.0-alpha5/browser-adaptive/
```

Native DPI, keyboard, High Contrast, and assistive-technology observations
remain open. This is Alpha 5 preparation evidence: no release identity,
package, tag, publication, feed, or protected branch changes here.
