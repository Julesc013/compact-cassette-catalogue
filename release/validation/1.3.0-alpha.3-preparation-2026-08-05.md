# C3 1.3.0 Alpha 3 preparation — 5 August 2026

Result: identity, classic-setup implementation/build/package controls, and
historical compiler-reconstruction preparation PASS. Historical Gate 1 remains
open for runtime workflow, catalogue-exchange, and visual/control evidence;
exact target-OS setup qualification and Candidate reproduction remain open.

## Release boundary

- Alpha 3 identity is `1.3.0a3 / Alpha 3 / v1.3.0a3`.
- Alpha 2 remains immutable at annotated tag object
  `0e8633671e55bbb7ce5e692f8e0f5c4201a62627`, targeting evidence commit
  `0aad46c6ad1d241caa70ceb74ccfe9bbfa12165b`.
- Alpha 3 descends from Alpha 2 post-tag attestation
  `a566b05014c5bac472be307c4a8328657bc47a6d`.
- `legacy/1.x` remains `c4115b82ea43fdd763685d862a08fe5c61db6dff`.
- No Alpha 3 tag, Candidate/retained Alpha distribution, public release, feed
  change, or legacy promotion is claimed. Ignored Preparation outputs are
  control-path evidence only and will be discarded before Candidate freeze.

## Source-only verification

The initial Alpha 3 source gate passed:

- baseline genome and compatibility corpus;
- documentation/link and exact three-lane identity validation;
- standalone catalogue characterization;
- package-set adversarial identity/lock cases;
- reproducibility-helper self-test; and
- actual target-tool parsing/execution under Windows PowerShell 2.

## Setup genetic baseline

`build/setup-genome.v1.json` freezes the surviving installer and uninstaller at
the Alpha 2 post-tag baseline before remediation. It records:

- both project GUIDs, assembly names, namespaces, startup objects, icons, and
  manifests;
- all six original form class identities;
- every original designer control name;
- all form/application resource keys; and
- exact lengths and SHA-256 values for the original artwork and icons.

`build/validate-setup-genome.ps1` now passes with seven reviewed differences:
two explicit administrator manifests and removal of five duplicate form-local
banner resources. Every difference has a named reason and mechanical regression
in `build/setup-genome-allowlist.json`; project, form, control, layout, artwork,
and recognizable wizard identity remain frozen.

## Historical Gate 1 boundary

The retained Microsoft-signed Build Tools 2015 Update 3 bootstrapper and both
official `v1.2.0b1` binaries reverified exactly. The signed x86 and amd64 MSI/CAB
payloads were extracted from that bootstrapper and expanded as administrative
images under the ignored compatibility-laboratory artifact root. This did not
install or register the retired product system-wide. The vendor MSI registry
table supplied the exact custom toolset properties; the x86 and amd64 files
were combined into one isolated MSBuild 14 toolset, with every contributing
file covered by the retained toolset-set hash.

`build/run-historical-gate1-builds.ps1` passed for immutable source commit
`2413e9139a098f3321385f2f946e743012a447f5` using exact MSBuild numeric and
product version `14.0.25420.1`. It rebuilt and structurally verified both
historical lanes:

| Lane | Rebuilt SHA-256 | Official SHA-256 | Byte-identical |
| --- | --- | --- | --- |
| x86/net40 | `54b0841ce718746978856494879b6a3f9404988ff13e1f7a31a34b06f202a01d` | `205ba251175d5a6fa20a3ace6127a00e5d10d73ad30581032c8f09b20ceb7222` | No |
| x64/net40 | `d5e9703796e0b9ec4110535d7a058792850e142a6a987f442b2b2f9180f20a4a` | `257ec9d0ea86f268d8328d71041e63eb379fc1809c91593db29d883359db747c` | No |

The rebuilt lengths match the corresponding official binaries and their
I386/PE32/ILONLY+32BITREQ and AMD64/PE32+/ILONLY architecture contracts pass.
The differing hashes are retained explicitly; byte identity is not claimed.
Exact tool paths, versions, hashes, the isolated toolset set hash, net40
reference-set hash, source identity, PE summaries, and official comparisons are
retained in ignored compatibility-laboratory evidence.

Application runtime source remains unchanged apart from the Alpha 3 stage and
informational-version projection. Gate 1 must still pass before the first
application behaviour patch.

The compiler-reconstruction portion of Gate 1 is therefore complete. The gate
itself remains open: historical launch and application workflows, private real
catalogue retention, screenshots/control evidence, and canonical catalogue
exchange with `v1.1.2` and both official `v1.2.0b1` architectures still require
their exact disposable runtime environments. No application behaviour patch is
authorized by this partial result.

## Setup lane characterization

Both setup projects and the solution now carry explicit Debug/Release
configurations for x86/net40, x64/net48, and ARM64/net481. Every mapping enforces
its exact `PlatformTarget`, framework, `Prefer32Bit=false`, warnings as errors,
and Release optimization. The setup-genome identity remained unchanged.

The initial pre-engine sweep reproduced the obsolete x86 `ZipFile` failure.
After removal of that online path and activation of the shared engine, the
current exact-source Preparation sweep at `b8bceb2...` passes all six binaries:

| Lane | Installer | Uninstaller |
| --- | --- | --- |
| x86/net40 | Builds and verifies I386/PE32/net40 | Builds and verifies I386/PE32/net40 |
| x64/net48 | Builds | Builds |
| ARM64/net481 | Builds and verifies `0xaa64`/PE32+/net481 | Builds and verifies `0xaa64`/PE32+/net481 |

Each build uses the lane's exact MSBuild/VBC/reference/ResGen authority, emits a
binary log and toolchain evidence, and has exactly one matching `.exe.config`
and zero runtime DLLs. This remains builder proof, not target-OS qualification.

## Shared offline payload verifier

Both setup projects now link `SetupShared/SetupContracts.vb` directly; no setup
DLL is produced. The implementation targets the net40 API floor and enforces:

- DTD/entity-disabled XML parsing and an exact schema/attribute shape;
- exact Alpha/product/lane/source fields ready for caller binding;
- a closed seven-file payload with case-collision rejection;
- canonical root-level names, invariant sizes, and lowercase SHA-256;
- exact extracted file count, length, hash, and reparse-point rejection; and
- local path containment with UNC/device/volume/system/profile-root rejection.

The legacy install wizard now loads only its adjacent version-bound manifest and
payload, revalidates before mutation, and invokes the shared transactional
operation. No `System.Net`, WebClient, archive-extraction, or ClickOnce runtime
path remains; a source gate closes the exact classic framework references and
source-identical shared implementation.

The shared engine now also writes and strictly rereads `C3.installed.xml`, with
exact product/source/lane/root/mode/transaction/file/registry/shortcut ownership
fields. The state file deliberately does not hash itself. A same-volume staged
file transaction supports clean install, repair, upgrade ordering, downgrade
and lane-change rejection, verified commit, and rollback. Eighteen setup tests
now pass, including preservation of an unknown catalogue, pre-mutation rejection
of an unowned name collision, and injected mid-commit restoration of the exact
prior payload and installed state. The shared removal transaction first verifies
every owned file against `C3.installed.xml`, stages only those owned files on the
same volume, preserves unknown files and a non-empty install directory, removes
the state file, and restores the exact installed state after an injected failure.
Modified owned files fail before mutation. Registry/shortcut mutation, process
handling, self-relocation, and both original wizard buttons are now composed
through the same shared contracts; real target-machine mutation remains open.

## Shared environment preflight

The shared .NET Framework 4.0-compatible preflight captures process and native
architecture, elevation, the Full Framework installation/release, the
operating-system Program Files directory, running C3 processes, and destination
volume free space. It closes the lane mapping, rejects emulation and
cross-architecture installation, enforces the lane's framework floor and an
elevated administrator token, rejects a running C3 instance, and reserves
three times the payload bytes for staging and rollback. It derives the default
product directory from the OS instead of the historical hard-coded x86 path.

Focused preflight regressions cover correct native architecture, emulation,
framework, elevation, running C3, and rollback space. Both application
manifests now require an administrator token with `uiAccess=false`, and the
legacy wizards invoke this preflight. This is still builder evidence rather
than a target-OS setup claim.

## Shared registry and shortcut ownership

The installed-state contract now derives one stable uninstall key per lane:
`CompactCassetteCatalogue-1x-x86`, `-x64`, or `-arm64`. The shared registry
service writes a closed Add/Remove Programs value set with quoted executable
paths and explicit state/lane/source ownership markers. It does not advertise
quiet uninstall or Control Panel repair before those entry points exist. An
unowned key collision is refused; removal requires an exact owned value set;
and rollback restores the prior owned values.

The shared shortcut service owns the mandatory common Start Menu link and the
optional common desktop link recorded in `C3.installed.xml`. It validates exact
paths, target, working directory, and description, refuses altered or unowned
links, verifies writes and removals, and restores previously removed links
after an injected failure. Its Windows implementation uses the system shortcut
provider without adding a runtime DLL.

Registry and shortcut services are composed with the file transaction and
activated by the legacy forms. No real-machine registry/shortcut qualification
is claimed until the exact three target environments pass.

## Atomic shared operations

`SetupInstallOperation` composes payload/environment validation, same-volume
file staging, installed-state creation, registry registration, and owned
shortcuts before the file backup is discarded. Registry or shortcut failure,
or a fault after complete system integration, invokes the retained rollback
actions and then restores the prior files/state. Repair can transactionally
change the optional desktop-shortcut selection while retaining the mandatory
Start Menu shortcut.

`SetupUninstallOperation` discovers the strict installed-state manifest, moves
owned files/state into same-volume quarantine, removes exact owned shortcuts and
the exact lane registry key, and deletes quarantine only after success. A fault
after system removal restores the files, state, registry, and shortcuts. An
altered registry binding blocks removal and restores any already removed
shortcuts; unknown catalogue files remain in the install directory.

## Executable setup, package, and UI closure

The setup contract suite now has 46 passing tests. The additional cases cover
exact uninstaller self-relocation, rejection of an altered relocated executable,
native uninstall preflight, a running-application block, exact adjacent Alpha 3
bundle loading, and rejection of a wrong release identity. The relocated worker
removes only authenticated ownership and uses delayed reboot cleanup only for
its own verified temporary executable/config pair.

`build/package-setup.ps1` constructs each deterministic ten-entry setup ZIP
from the already-verified portable ZIP entries plus the matching verified setup
outputs. Generated `payload.xml` is a canonical byte projection of the exact
seven installed files. `build/verify-setup-packages.ps1` binds every entry,
portable source package, setup executable, source commit, and toolchain lock.
Four adversarial cases pass: altered portable payload, altered `SETUP.exe`,
altered release identity, and an extra bundle entry are rejected even after all
attacker-controlled checksum sidecars are rewritten.

The full Preparation packaging path passed once from source `b7c0f09...`,
producing three closed portable and three closed setup ZIPs and assembling the
exact six-archive/one-checksum release set. Subsequent artwork/accessibility
commits intentionally make those ignored bytes stale, so they are not retained
or claimed; all assets must be rebuilt from the final frozen source.

Five 9.3-million-character duplicate serialized banner resources were removed.
The five existing picture controls now use the unchanged canonical project
resource, whose SHA-256 is
`6ea7ebb8d5b0621a2c8cc9f6819fd3e7686382f62feeb3b84aaa301baae8f124`.
All six setup executables rebuild and verify after that change. Keyboard
mnemonics, dynamic default buttons, accessible directory/status/progress names,
and explicit completion/failure semantics are mechanically required.

Open boundaries are exact target-OS install/repair/upgrade/uninstall/fault and
accessibility/DPI/high-contrast runs, two clean path-distinct Candidate builds,
the final external lock, and retention/tag attestation. Historical Gate 1 also
continues to prohibit application behaviour changes.
