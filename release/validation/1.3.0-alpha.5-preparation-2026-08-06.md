# C3 1.3.0 Alpha 5 source-side preparation — 6 August 2026

Result: **PASS for planning, inventory, and implementation readiness only.**

No topology move, application/layout implementation, new test executable,
build, package, external lock, runtime qualification, Alpha 5 identity, tag, or
publication is claimed by this record.

## Starting boundary

Preparation began from clean synchronized `dev/1.x` at Alpha 4 post-tag
attestation `2ddcebc26a8486cd3192ab919d1117d21b4a5a92`.

Immutable branch/tag boundaries at entry:

| Surface | Object |
| --- | --- |
| Alpha 4 annotated tag object | `8153f56544cff812fc8cf319a06224da563c3faf` |
| Alpha 4 tag target/evidence commit | `08a0da3` |
| `legacy/1.x` | `c4115b82ea43fdd763685d862a08fe5c61db6dff` |
| `master` | `050b2287eed71e12b78136e5f87ec4fb0ef268cf` |
| `dev/2.x` | `ac9b598cb5a971032c832f4c969410007ca4d080` |

The first two preparation commits are:

| Commit | Purpose |
| --- | --- |
| `18a1cb8` | ratify Alpha 5 layout/topology contracts and immutable visual references |
| `835eafa` | insert Alpha 5 into recovery, milestone, Beta, and qualification authorities |

## Owner-test diagnosis retained

The source inventory confirms the owner report:

- `CatalogueUx` enables form-level scrolling and runtime anchoring;
- it creates static Cancel/inline/list actions with literal bounds;
- `frmTapeNew` changes Model width and positions three commands at runtime;
- `frmModelNew` changes client/group geometry during `Load`;
- the four entity browsers move `grpActions` and all children during `Load`;
- `frmMain` creates and positions its empty state dynamically; and
- the current characterization calls those helpers but does not resize forms
  and inspect sibling geometry.

This justifies Alpha 5 without changing Alpha 4's immutable evidence.

## Source-topology inventory

The `src/` observed after the sole local checkout switched from `dev/2.x` to
`dev/1.x` contained no tracked 1.x source. It held ignored 2.x `bin/obj`
outputs. A scoped `git clean -ndx -- src/` named only `src/`; the corresponding
`git clean -fdx -- src/` removed that reproducible residue.

The actual hybrid production source is `SetupShared/`: 13 VB.NET files linked
directly into Installer, Uninstaller, and `C3.Setup.Characterization`. The
ratified 1.x topology keeps the three original product project roots and moves
the byte-identical canonical shared engine to
`Compact Cassette Catalogue Installer/Shared/`. That move is implementation
work and remains open.

## Prepared authorities

- owner decision distinguishing pixel, metric, and semantic fidelity;
- immutable visual inheritance contract and C3 96-DPI metric grid;
- exact mandatory and conditional form scope;
- role-specific layout/container policy;
- two-stage structural/adaptive form method;
- original-simple source-topology transaction;
- fresh-process size/scale/content geometry specification;
- source-policy, text, keyboard, UIA/MSAA, High Contrast, and native visual
  authority;
- lifecycle/layout performance contract;
- independent `UI-START-001` launch instrumentation gate;
- form-by-form implementation/evidence ledger;
- exact six-asset Alpha 5 release transaction; and
- explicit human approval boundary for later Beta-labelled operations.

The historical demonstration screenshot is frozen locally with SHA-256
`f926fc644b0a2bbd79f2f4d0f2a4465c7b9e0eb4e0ed90ab7d884ddf56b1ba27`.
Its manifest states that the image shows 1.1.2 and is a visual inheritance
oracle, not v1.2 execution proof.

## Verification result

`build/verify-preparation.ps1 -SkipBuildOutputs` passed after staging this
record. It reverified the 13-surface baseline genome with the four existing
reviewed differences, compatibility corpus, 64 tracked Markdown documents and
local links, exact three-lane identity, setup manifests/offline/journal/
artwork/accessibility controls, and all 130 current characterization cases:

```text
31 application lifecycle/catalogue/workflow
8  referential/counter integrity
6  settings/diagnostics
85 setup durability/adversarial
```

The current `legacy windows are resizable and scroll safe` property case still
passes. That is retained as the known Alpha 4 false positive; it does not fill
any Alpha 5 geometry or native visual row.

## Implementation entrance

The next tracked change is one focused topology transaction. It must move the
shared source without content edits, update project/test/tool/document paths,
prove exact source membership and narrow genome differences, and pass setup
plus complete source gates before any Designer file changes.

Layout work then begins test-first with retained Alpha 4 failing geometry. The
repository stays at `1.3.0a4 / Alpha 4` identity until the final Alpha 5 source
freeze. `v1.3.0a4`, prior Candidate evidence, `master`, `legacy/1.x`,
`dev/2.x`, feeds, and public releases remain unchanged.
