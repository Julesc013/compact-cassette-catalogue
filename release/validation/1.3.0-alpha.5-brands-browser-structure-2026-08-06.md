# C3 1.3.0 Alpha 5 Brands browser structure — 6 August 2026

Result: **PASS for the Brands browser Stage A hierarchy.**

`frmBrands` is the first implementation of the shared browser ownership
contract:

```text
splitBrowseRoot
├── Panel1 (fixed, AutoScroll)
│   └── grpFilters
└── Panel2
    └── tlpBrowseRight
        ├── grpModels
        │   └── lstBrands (Dock Fill)
        └── tlpBrowseFooter
            ├── flpBrowseStatus
            │   ├── lblResults
            │   └── txtResults
            └── grpActions
```

The form no longer scrolls and behavior source no longer invokes
`CatalogueUx.ConfigureListForm`. The original filters, ListView, columns,
buttons, messages, and event handlers remain. The former duplicate action tab
indexes are now a unique Add/Refresh/Edit/Delete sequence.

A characterization case closes every parent boundary, list fill, form scroll
policy, and absence of the runtime helper. The exact control-name allow-list
contains 36 reviewed additions with zero baseline removals. Focused 800×552
ordinary and 200% maximum-text processes pass the current geometry assertions.
All 138 characterization cases must pass before accepting the source commit.

The action group remains vertically arranged in Stage A. Its adaptive command
row, complete browser atlas, and native review remain open; this record does
not qualify Brands or Alpha 5.

No release identity, package, tag, publication, feed, or protected branch
changed.
