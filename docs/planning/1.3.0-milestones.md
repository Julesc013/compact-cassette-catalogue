# C3 1.3.0 milestone ladder

The final original C3 release uses an explicit evidence ladder. A later stage
inherits every earlier gate; it does not reinterpret missing evidence as passed.
The [post-Alpha owner decision](../governance/1.3.0-three-lane-matrix-2026-08-05.md)
replaces Alpha's four diagnostic lanes with exactly three Beta/stable release
lanes while preserving the immutable Alpha record.

The [preview release authorization](../governance/1.3.0-release-authorization-2026-08-05.md)
permits deliberate Alpha tags and retained Alpha distributions after their
checks. The [6 August Beta decision](../governance/1.3.0-beta1-authorization-2026-08-06.md)
additionally authorizes retained `1.3.0b1` Candidate bytes now, and authorizes
the Beta tag plus lease-protected `legacy/1.x` advancement only on complete
technical GO. Public publication remains separately unauthorized.

The [6 August bounded legacy usability amendment](../governance/1.3.0-bounded-legacy-ux-2026-08-06.md)
adds the final original-WinForms creation, layout, keyboard, and accessibility
hardening before Beta target qualification. It explicitly permits one reviewed
workflow source file and bounded form/control genome differences without
opening the 2.0 frontend scope.

The later [Alpha 5 layout decision](../governance/1.3.0-alpha5-layout-stabilization-2026-08-06.md)
preserves those workflows but replaces the insufficient runtime
anchor/form-scroll geometry. The
[source-topology decision](../governance/1.3.0-source-topology-normalization-2026-08-06.md)
also restores the original-simple three-project source tree before Designer
work begins.

| Milestone | Tag | Purpose | Publication |
| --- | --- | --- | --- |
| Alpha 1 | `v1.3.0a1` | Freeze the corrected three-anchor ancestry, maintenance doctrine, compatibility evidence, and validation/build scaffolding | Annotated source tag only; intentionally unpublished |
| Alpha 2 | `v1.3.0a2` | Prove the authoritative three-lane build, immutable-lock, deterministic-package, and builder-smoke control path | Retained engineering ZIPs; intentionally unpublished |
| Alpha 3 | superseded before tag | Internal implementation phase for legacy reliability repairs and the offline classic setup | No separate tag/package wave; scope flows into Beta 1 |
| Alpha 4 | `v1.3.0a4` | Immutable owner-test discovery checkpoint that exposed the inadequate absolute/runtime-anchor layout mechanism | Retained local test ZIPs; intentionally unpublished and never replaced |
| Alpha 5 | `v1.3.0a5` | Normalize the legacy source topology, reconstruct role-appropriate native layouts, and qualify real geometry/accessibility/startup behaviour | Retained local test ZIPs after the complete Alpha gate; intentionally unpublished |
| Beta 1 | `v1.3.0b1` | Freeze and qualify the owner-accepted Alpha 5 functional source under Beta identity | Human approval required for Beta-labelled artifacts/tag; public prerelease separately unauthorized |
| Stable | `v1.3.0` | Rebuild and fully qualify the accepted Beta source under stable identity, publish retained bytes, verify downloads, and promote the legacy feed | Public stable release |

There is no C3 1.2.1 release. There is no planned feature milestone after 1.3.0.

## Alpha 1 exit

Alpha 1 proves that future work starts from the right program and is guarded
against architectural drift. It does not claim that known baseline defects are
fixed or that any operating-system support matrix has been rerun.

Required:

- direct development ancestry from `58a5b7d...`;
- exact runtime genome from `509c9ec...` and release evidence at `v1.2.0b1`;
- immutable reachability of the superseded refactor tip;
- complete maintenance governance, roadmap, TODO, changelog, notes, and plans;
- projected historical catalogue/settings evidence and strict validation;
- passing baseline-genome gate with no undocumented exception;
- source-identical four-lane build configuration;
- diagnostic compilation and binary metadata verification on available tools;
- exact verification of official `v1.2.0b1` assets; and
- an evidence record that lists every deferred authoritative/manual gate.

The four-lane item above is an Alpha historical result, not a continuing
publication requirement.

## Alpha 2 exit

Alpha 2 proves that the ratified three-lane release machinery can produce an
honestly labelled, retained engineering checkpoint before runtime repair. It
requires serviced builders, one clean pushed source, one external immutable
lock, Preparation and Candidate builds, final source closure, exact `1.3.0a2`
package identity, two path-distinct source rebuilds, x86/x64 builder smoke, an
annotated evidence-complete tag at `E`, and direct post-tag attestation `P`.
Historical Gate 1, application
repairs, native ARM64 runtime execution, and minimum-OS qualification remain
explicitly deferred.

## Alpha 3 entrance and exit

Alpha 3 descends from the Alpha 2 post-tag attestation and remains the internal
implementation phase. The 6 August amendment allows reproduction-backed
application work to proceed while external Gate 1 execution is completed, but
does not waive Gate 1 for Beta GO. Alpha 3 completes the approved S0-S2
reliability sequence and revives the original classic setup as an optional,
offline, three-lane wrapper around exact portable payload bytes.

It exits only with:

- Gate 1 historical reconstruction and exchange evidence complete;
- every reproducible S0-S2 application defect resolved with focused regression
  evidence;
- expanded XML, persistence-fault, randomized-invariant, and lifecycle tests
  passing;
- the installer and uninstaller retaining their genetic identity while sharing
  one linked VB.NET implementation and adding no runtime DLL;
- clean install, repair, upgrade, rollback, uninstall, repeated-uninstall,
  locked-file, and user-data-preservation evidence on all three targets;
- deterministic portable and optional setup bundles from two clean paths;
- native target-machine qualification for x86, x64, and ARM64; and
- all repository-side regressions passing before the single metadata-only Beta
  projection. Because `v1.3.0a3` was never created, no separate Alpha 3
  `C -> E -> tag -> P` or package wave is required.

## Alpha 4 exit and discovery result

Alpha 4 froze the repository-complete application/setup source into six
reproducible, retained test ZIPs. It passed source, toolchain, binary, package,
and path-distinct reproduction controls. Owner testing then demonstrated that
the property-only layout test was a false positive: fixed siblings overlap,
runtime-created buttons do not share Designer scaling/parentage, and form-level
scrolling conflicts with anchored children.

That is a successful discovery result, not permission to edit the Alpha 4 tag
or bytes. Alpha 5 is the required successor.

## Alpha 5 entrance and exit

Alpha 5 begins at the direct descendant of the Alpha 4 post-tag attestation.
It first restores the original-simple three-project topology by moving the
byte-identical shared setup engine under the Installer directory. It then
reconstructs mandatory forms through separate structural and adaptive stages.

It exits only with:

- root `src/` and `SetupShared/` absent from the 1.x production topology;
- original application, installer, and uninstaller source roots preserved;
- Alpha 4 layout failures reproduced by a fresh-process STA geometry harness;
- runtime static commands moved into Designers and geometry handlers removed;
- every mandatory form passing the size/scale/content matrix;
- conditional forms changed only for retained reproduced defects;
- canonical Windows 7/96-DPI visual approval and real-DPI/theme evidence;
- complete keyboard, UIA/MSAA, screen-reader, and High Contrast evidence;
- bounded layout/lifecycle performance and no duplicate controls/subscriptions;
- `UI-START-001` resolved or retained as an explicit Alpha blocker;
- narrow reviewed genome differences rather than regenerated baselines;
- all application/setup/source/package/reproduction controls passing again;
- exactly six Alpha 5 ZIPs retained from two clean path-distinct builds; and
- annotated Alpha tag plus direct post-tag attestation without publication or
  branch/feed movement.

## Beta 1 entrance and exit

Beta projection begins only after the Alpha 5 source and exact retained bytes
pass owner testing plus every inherited external gate. Beta 1 adds no new
behaviour; it is the accepted Alpha 5 source under an audited metadata-only
identity. Beta-labelled artifacts and the tag remain human-controlled
operations; public publication remains separately gated.

It exits only with:

- every S0–S2 item in the salvage ledger resolved or explicitly rejected after
  reproduction proves it does not exist;
- Gate 2 lifecycle/data-safety work complete;
- Gate 3 referential/counter work complete;
- Gate 4 settings migration complete;
- Gate 5 lanes and portable packages complete;
- native ARM64 AA64 output and Windows-on-ARM target execution proved;
- automated historical and reverse-compatibility matrices passing;
- required manual workflows passing on retained package bytes;
- optional classic setup bundles, when retained for Beta, consuming the exact
  qualified portable bytes and passing the full install/uninstall matrix;
- no new runtime DLL, catalogue format, project split, or UI architecture; the
  only source-topology/UI differences are those reviewed under the bounded
  legacy usability amendment; and
- a complete Beta validation record with exact source, package, and toolchain
  identities; and
- complete historical, reproducibility, runtime, and setup target evidence
  before the conditionally authorized Beta tag or legacy-ledger advancement.

## Stable entrance and exit

Stable starts from an owner-accepted Beta 1 source. The stable source transition
may change version/stage/date and release-facing projections only. A functional
change requires a new beta checkpoint rather than a silent stable repair.

Stable exits only after two clean path-distinct builds, deterministic package
comparison, the full OS/manual matrix, immutable tagging, publication of the
already-qualified bytes, clean re-download verification, and final feed/branch
promotion.

## Cross-milestone risks

| Risk | Control | Release impact |
| --- | --- | --- |
| Refactor architecture leaks into 1.x | genome manifest, original-simple three-project topology, diff review | Blocks commit |
| Hybrid root source layout returns | prohibit production `src/` and root `SetupShared/` on 1.x | Blocks Alpha 5 |
| Property-only layout proof hides overlap | fresh-process geometry/text/reachability matrix plus native review | Blocks Alpha 5/Beta |
| Layout work introduces startup loops | `UI-START-001` milestone instrumentation and repeated launch matrix | Blocks Alpha 5/Beta |
| A failed load/save damages active data | temporary state, transactional save, fault tests | Blocks Beta/stable |
| Old settings silently reset | real isolated profiles plus fixture validation | Blocks Beta/stable |
| Newer frameworks change behaviour | identical source/resources and cross-lane exchange | Blocks affected lane |
| ARM64 silently falls back to x64/AnyCPU | closed PE/CorFlags inspection plus native target-process proof | Blocks Beta/stable |
| Preview metadata advertises unavailable bytes | feed isolation validator | Blocks tag/publication |
| Qualification rebuild changes bytes | retain artifacts; publish without rebuilding | Blocks publication |
| Classic Windows claim lacks evidence | stage-specific OS matrix and explicit deferral | Claim removed or release blocked |
| Candidate lock is self-referential, stale, or detached from source | immutable external lock, clean remote-bound source, retained lock SHA-256 | Blocks candidate build |
| Three individually valid packages use different release/source/lock identities | one closed package-evidence set with one stage label, source, and lock | Blocks package creation |
| Provider ref was stale at freeze | explicit fetch receipt captured before offline builds | Blocks lock capture |
| Candidate builds mutate or drift from frozen source | final source/ref/genome/lock closure after all lanes | Blocks Candidate packaging |
| Extracted directory does not come from verified ZIP | authenticated five-entry manifest with length/SHA-256 closure | Blocks target launch |

The detailed work and evidence live in the milestone-specific plans and the
qualification matrix.
