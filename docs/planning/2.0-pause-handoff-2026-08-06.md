# C3 2.0 development pause handoff — 2026-08-06

Status: **Safe verified integration checkpoint; development deliberately paused**

This is a resumption record, not another product plan. The source-of-truth order
remains:

1. `release/train/2.0.0.json`;
2. [2.0 execution plan](2.0-execution-plan.md);
3. [catalogue and Application architecture](../architecture/catalogue-and-application.md);
4. applicable ADRs and concern-specific contracts;
5. [2.0 grand programme](2.0-grand-programme.md); and
6. `ROADMAP.md` and the root `README.md` as projections.

## Exact checkpoint

At handoff preparation:

| Authority | Identity/state |
| --- | --- |
| Active milestone | `2.0.0-alpha.6` |
| Latest implementation commit | `148158d7ace20da627f06a11d9bccb97022d572c` |
| Qualified ledger `master` | `050b2287eed71e12b78136e5f87ec4fb0ef268cf` |
| Latest qualified tag | `2.0.0a5` |
| Alpha 5 source `C` | `a7a26803f75857716866846f3cdccfb0665f1cc2` |
| Alpha 5 evidence/tag target `E` | `cab1afabc25cf01745ed93dc4ae03c031e5fe362` |
| Alpha 5 post-operation `P` | `050b2287eed71e12b78136e5f87ec4fb0ef268cf` |
| Alpha 5 annotated tag object | `a35e50d9d931b5bb8eb059e20598bfe1249822ce` |
| Alpha 6 candidate | Not frozen; `candidateCommit: null` |
| Alpha 6 tag | Absent |
| Publication | Intentionally unpublished; no feed promotion |

The documentation commit containing this record is the intended final paused
`dev/2.x` head. Resolve its exact SHA from `git rev-parse dev/2.x`; a commit
cannot truthfully contain its own identity.

## Verified implementation now present

Alpha 6 has a complete immutable format-neutral graph for metadata, Brands,
cassette models, deck models, physical deck units, Tapes, sides, and recordings.
It enforces typed keys, deterministic ordering, relationship resolution, and
single side ownership of every recording. Snapshot, transaction, query, cursor,
resource-budget, fingerprint, and fingerprint-delta contracts remain
non-production convergence infrastructure.

The current adapter flow is:

```text
validated legacy DataSet -> canonical CatalogueState
native-v2 DTO             -> canonical CatalogueState
canonical CatalogueState  -> native-v2 DTO
canonical CatalogueState  -> loss-aware legacy export facade -> legacy DataSet
```

Native-v2 round trips retain canonical fingerprints and deterministic XML bytes.
Legacy migration preserves the accepted deterministic IDs and timestamp
normalizations. Canonical state retains and fingerprints the legacy cassette-
model sequence counter; strict ordinary native adaptation refuses its loss,
while migration reports the frozen native-v2 limitation explicitly. Native DTO
legacy-export entry points now adapt through canonical state and no longer own a
second direct field map.

The final full repository verification before this pause passed:

- 98 characterization scenarios;
- 661 Catalogue API signatures;
- 488 Infrastructure API signatures;
- x86/.NET Framework 4.0 and x64/.NET Framework 4.8 WinForms builds;
- eight CLI process scenarios;
- migration, legacy export, interruption recovery, and compatibility checks;
- release, distribution, update, and branch contracts; and
- binary identity and PE architecture checks.

## Deliberately unfinished Alpha 6 work

Alpha 6 remains active and unqualified. Resume in this dependency order:

1. Separate the general legacy-profile projection result from mutable
   `MigrationReport` ownership.
2. Freeze fixed deterministic-ID vectors before replacing its transitional
   Native marker-type coupling.
3. Extract a typed canonical-to-v1.1 profile adapter result containing key
   mappings, normalizations, losses, unsupported findings, and blocking issues;
   keep `LegacyXmlCatalogueStore` as the only XML and transactional I/O owner.
4. Run shared legacy/native adapter conformance over blank, populated,
   historical, culture, boundary, corruption, security, and refusal fixtures.
5. Run whole-document differential proof against the current production legacy
   projections without changing production mutation ownership.
6. Add negative architecture-fitness scenarios that confine `DataSet`, XML, and
   Native DTOs to the approved profile/transition boundaries.
7. Reconcile all Alpha 6 documentation and machine contracts, run the full gate,
   then—and only then—freeze and promote exact Alpha 6 C/E/P with tag `2.0.0a6`.

Do not create `C3.Application`, open Alpha 7, change the production mutation
owner, freeze an Alpha 6 candidate, or create an Alpha 6 tag before these gates
pass.

## Later programme retained

The accepted sequence remains Alpha 7 Application ownership, Alpha 8 permanent
Windows shell, Alpha 9 complete goal-first Windows workflows, Alpha 10
Application-backed CLI, Alpha 11 qualified TUI or explicit deferral, Alpha 12
convergence/hardening, then exact Beta 1 owner qualification. The canonical
goal-first rule remains: expected missing prerequisites produce actions rather
than error-dialog dead ends; parent drafts survive continuations; one accepted
task commits atomically and creates one semantic undo entry.

Beta 1 remains the first consolidated owner manual gate for exact retained
packages. No absent DPI, high-contrast, screen-reader, minimum-OS, or owner
usability evidence may be recorded as passed before then.

## Local recovery evidence

The interrupted pre-commit state was preserved locally at:

```text
artifacts/recovery/vscode-close-20260806T120821Z/
```

It contains ref/log/reflog evidence, a 45,928-byte binary unstaged patch, an
untracked-file manifest, safe copies, and SHA-256 hashes. Local rescue branch:

```text
rescue/vscode-close-20260806T120821Z
```

The repository also contains unrelated untracked `.vscode/` and `tmp/` content.
It was preserved, excluded from C3 commits, and must not be deleted or adopted
without separate owner review.

## Resume preflight

When development resumes:

```powershell
git fetch origin --prune --tags
git status --short --branch
git rev-parse dev/2.x
git rev-parse origin/dev/2.x
git rev-parse master
git rev-parse origin/master
git tag --list 2.0.0a6
```

Expected invariants are a clean tracked worktree, identical local/remote
`dev/2.x`, `master` still at Alpha 5 `P`, active Alpha 6 train state, no Alpha 6
candidate, and no `2.0.0a6` tag. Preserve and inspect any difference before
continuing; never reset, clean, rewrite, force-push, or move a qualified tag.
