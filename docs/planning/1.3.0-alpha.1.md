# C3 1.3.0 Alpha 1 checkpoint

Tag: `v1.3.0a1`

Policy: intentionally unpublished source checkpoint; no GitHub binary release,
no update-feed promotion, and no `legacy/1.x` promotion.

## Included

- one-time recovery of the 1.x development lineage from `58a5b7d...`;
- exact runtime-genome authority from `509c9ec...` and qualified release
  authority from `v1.2.0b1` / `2413e913...`;
- annotated archival preservation of the 1.2.1/refactor attempt;
- complete conservative maintenance doctrine and branch governance;
- itemized salvage ledger for all known repair candidates;
- exact catalogue 1.1.0 specification, historical fixtures, settings fixtures,
  release provenance, and strict corpus validation;
- generated baseline-genome manifest and empty default allow-list;
- standalone XML characterization tests;
- one-project x86/net40, x64/net40, x64/net48, and x64/net481 build lanes;
- PE/framework/settings/payload verification; and
- reproducible official-baseline download and hash verification.

## Explicitly not included

- lifecycle, persistence, tape, referential, counter, or settings runtime repair;
- final VS2015/MSBuild 14 reconstruction;
- full historical reader/writer execution;
- complete manual workflow or OS qualification;
- portable 1.3 packages;
- a public prerelease; or
- any update-feed change.

## Alpha validation

Run from the exact source commit:

```powershell
.\build\build.ps1 -Configuration Release -Rebuild -AllowCompatibleFallback
.\build\verify-alpha1.ps1 -Configuration Release
```

The fallback flag is diagnostic for net40 on a machine without MSBuild 14. Its
warning and deferred authority must remain in the validation record.

## Tag gate

- working tree clean;
- annotated tag absent locally and remotely;
- `dev/1.x` at the exact candidate commit;
- source identity `1.3.0 / Alpha 1`, catalogue format `1.1.0`;
- public root feed still exactly `1.2.0 / Release / 14/05/2026`;
- all automated Alpha checks pass;
- diagnostic build evidence recorded honestly; and
- validation record names the exact candidate commit and known deferrals.

The annotated tag message states that Alpha 1 is a qualified maintenance
foundation checkpoint, intentionally unpublished and not runtime-complete.
